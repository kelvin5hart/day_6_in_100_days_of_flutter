import 'package:flutter/material.dart';

//color
const iconColor = Color(0xFF384046);
const secondaryColor = Color(0xFF31373D);
const primaryColor = Color(0xFFBBF146);
const backgroundGradient = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomCenter,
    colors: [ Color(0xFF31373D),  Color(0xFF384046), ],
    stops: [ 0.55, 1]);
